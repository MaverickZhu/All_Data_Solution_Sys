# All Data Solution Sys - TODO List

为下一阶段的开发工作制定清晰的路线图，确保项目高效、有序地推进。

## 优先级最高的任务 (Next Sprint)

### P0: 前端集成与用户体验闭环
*   [ ] **对接异步数据分析API**:
    *   [ ] 修改前端页面，将"数据分析"按钮的点击事件，从调用旧的同步API改为调用新的 `POST /api/v1/processing/profile/{data_source_id}` 接口。
    *   [ ] 在状态管理中保存接口返回的 `task_id`。
*   [ ] **实现任务状态轮询与展示**:
    *   [ ] 发起分析后，前端页面应显示"分析中..."的加载状态，并禁用相关操作。
    *   [ ] 使用 `task_id` 定期（例如每3秒）轮询 `GET /api/v1/processing/profile/{task_id}` 接口，获取任务最新状态。
    *   [ ] 当轮询发现 `profiling_status` 变为 `completed` 或 `failed` 时，停止轮询。
*   [ ] **分析结果可视化**:
    *   [ ] 当任务成功时 (`completed`)，获取 `profiling_result` 中的JSON数据。
    *   [ ] 使用 `echarts` 或 `plotly` 等图表库将分析报告可视化地呈现在前端页面上。
*   [ ] **友好的错误处理**:
    *   [ ] 当任务状态为 `failed` 时，在前端界面上给出明确、易于理解的错误提示。


## P1: 后端能力扩展与健壮性提升

*   [ ] **扩展核心分析能力**:
    *   [ ] **引入文本分析任务**: 为上传的 `.txt`, `.csv` 文件增加新的异步分析任务（如：主题建模、关键词提取、情感分析），并扩展API。
    *   [ ] **引入图像分析任务**: 为上传的 `.jpg`, `.png` 文件增加异步分析任务（如：使用 `imagehash` 生成图像指紋查找相似图片），并扩展API。
*   [ ] **提升系统健壮性**:
    *   [ ] 为Celery任务增加自动重试机制，以应对网络抖动或数据库临时不可用的情况。
    *   [ ] 将详细的错误堆栈信息记录到日志文件中，方便排查问题。
*   [ ] **编写后端自动化测试**:
    *   [ ] 为 `processing` API 编写集成测试，覆盖"启动任务 -> 查询状态 -> 获取结果"的完整流程。
    *   [ ] 为 `run_profiling_task` 等核心Celery任务编写单元测试（使用Mock）。


## P2: 探索核心AI功能：语义检索 (RAG)

*   [ ] **设计并实现Embedding流程**:
    *   [ ] 设计一个流程，当文件上传成功后，自动将其内容分块（Chunking）。
    *   [ ] 创建一个新的Celery任务，调用预训练模型（如 DeepSeek）为文本块生成向量嵌入。
*   [ ] **实现向量索引与存储**:
    *   [ ] 将生成的向量嵌入和相关的元数据（如`data_source_id`, `chunk_id`）存入 Milvus 向量数据库。
*   [ ] **构建基础检索API**:
    *   [ ] 创建一个新的API端点，接收用户的自然语言查询。
    *   [ ] 将查询文本转换为向量，从 Milvus 中检索最相关的Top-K个文本块并返回。 